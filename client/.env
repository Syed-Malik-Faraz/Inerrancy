# Development: use the Vite proxy (vite.config.js proxies /api → localhost:5001).
# This keeps requests same-origin so SameSite=Lax cookies work and CORS is avoided.
# For production, set this to your deployed API URL (e.g. https://api.yourdomain.com/api)
# in a .env.production file.

VITE_API_URL=/api

VITE_GOOGLE_CLIENT_ID=930834663241-pic2l5hnnlroruk6rb7mlc7mm4gap2i1.apps.googleusercontent.com
